/*Ricorsione per trovare la lunghezza di un numero
*/
#include <stdio.h>
#include <string.h>

unsigned numDigits(const unsigned n) {
    // if (n < 10) return 1;
    // return 1 + numDigits(n / 10);
    int sum = 0;
    while(getchar()!=0){
        scanf
    }
}

